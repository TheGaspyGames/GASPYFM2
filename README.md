# GASPYFM Radio TTS

## Cambios
- Reemplazado `edge-tts` por `node-edge-tts` para evitar el error de Node 24 con paquetes TypeScript dentro de `node_modules`.
- TTS de boletines y saludos sigue funcionando sin cuenta ni API key.

## Instalación
1. Copia `.env.example` a `.env`.
2. Ejecuta `npm install`.
3. Activa Companion Server en YTMDesktop.
4. Ejecuta `npm start`.
